import SocketService from "./socket.service";

const signUp = () => {
    // SocketService.emit()
}

const signIn = () => {

}

const AuthService = {
    signIn,
    signUp,
}

export default AuthService;